<%= form_with(model: megacity, local: true) do |form| %>
  <% if megacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(megacity.errors.count, "error") %> prohibited this megacity from being saved:</h2>

      <ul>
      <% megacity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
